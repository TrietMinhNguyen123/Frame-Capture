# 🌋 04. Video-Filled Element Text And Matching Audio

<p style="color:#6ee7b7;"><strong>Goal:</strong> Start from the end of Part 03, then make each element word fill with its matching video and play its matching audio.</p>

<details open>
<summary><strong>🗺️ Big Picture</strong></summary>

At the end of Part 03, `interactive.js` only draws plain white text.

In this part, we keep the same controls:

1. The right hand switches the word.
2. The left hand scales the word.

But now:

1. The word is filled with video instead of plain white.
2. The video changes when the word changes.
3. The audio changes when the word changes.
4. The left hand also controls audio volume.

The main trick is that the element name chooses the file.

For example:

```js
"FIRE".toLowerCase()
```

becomes:

```txt
fire
```

So we can build:

```txt
assets/fire_640.mp4
assets/fire.mp3
```

<p style="color:#fbbf24;"><strong>💡 Keep it simple:</strong> This version does not do the full-screen video transition. It only does video inside the word and matching audio.</p>

</details>

---

<details>
<summary><strong>🧊 <span style="color:#60a5fa;">Step 1: Check The Assets Folder</span></strong></summary>

Open the `assets` folder.

You should see these files:

```txt
assets/earth_640.mp4
assets/earth.mp3
assets/wind_640.mp4
assets/wind.mp3
assets/fire_640.mp4
assets/fire.mp3
assets/water_640.mp4
assets/water.mp3
```

The names are important because our code will build the filenames from the element word.

For example, if the current element is:

```js
"WATER"
```

then:

```js
elementName.toLowerCase()
```

turns it into:

```js
"water"
```

Then the code can make:

```js
"assets/" + elementName.toLowerCase() + "_640.mp4"
"assets/" + elementName.toLowerCase() + ".mp3"
```

</details>

<details>
<summary><strong>🧱 <span style="color:#f472b6;">Step 2: Add Video And Audio Variables</span></strong></summary>

Open `interactive.js`.

At the top, you should already have the Part 03 variables.

They probably look like this:

```js
// old code ⬇️
let elements = ["EARTH", "WIND", "FIRE", "WATER"];
let currentElementIndex = 0;
let elementScaleAmount = 0.5;
let minElementTextSize = 12;
let maxElementTextSize = 360;
// old code ⬆️
```

Keep those variables, but organize them and add the new empty objects underneath.

Replace the top of `interactive.js` with this:

```js
// new code ⬇️
let elements = ["EARTH", "WIND", "FIRE", "WATER"];
let currentElementIndex = 0;

// text scaling vars
let elementScaleAmount = 0.5;
let minElementTextSize = 12;
let maxElementTextSize = 360;

// video vars
let elementVideos = {};
let elementTextLayer;

// audio vars
let elementAudios = {};
let audioStarted = false;
// new code ⬆️
```

What these new variables mean:

- `elementVideos` stores the videos for `EARTH`, `WIND`, `FIRE`, and `WATER`.
- `elementTextLayer` is a hidden drawing layer where we make the video-text mask.
- `elementAudios` stores the sounds for `EARTH`, `WIND`, `FIRE`, and `WATER`.
- `audioStarted` remembers whether the user has clicked once to allow sound.

<p style="color:#86efac;"><strong>✨ Tiny pattern:</strong> Both videos and audio are stored by element name, so <code>elementVideos["FIRE"]</code> gets the fire video and <code>elementAudios["FIRE"]</code> gets the fire sound.</p>

</details>

<details>
<summary><strong>🎬 <span style="color:#fb7185;">Step 3: Create A Setup Function For The Media</span></strong></summary>

Now add a new function called `setupInteractive()`.

This function does not replace an old Part 03 function.

It goes in `interactive.js` after the top variables and before `advanceElement()`.

Look for this area:

```js
let audioStarted = false;

// new code goes here ⬇️

function advanceElement() {
  currentElementIndex++;
```

Put this new function in that spot:

```js
// new code ⬇️
function setupInteractive() {
  for (let i = 0; i < elements.length; i++) {
    let elementName = elements[i];

    // switch out video for each element
    let videoPath = "assets/" + elementName.toLowerCase() + "_640.mp4";

    elementVideos[elementName] = createVideo(videoPath);
    elementVideos[elementName].hide();
    elementVideos[elementName].volume(0);
    elementVideos[elementName].loop();
  }

  elementTextLayer = createGraphics(width, height);
}
// new code ⬆️
```

Then your file should flow like this:

```js
let audioStarted = false;

// new code ⬇️
function setupInteractive() {
  // media setup goes here
}
// new code ⬆️

function advanceElement() {
  currentElementIndex++;
```

This loop creates one video for each element.

For `FIRE`, this line:

```js
let videoPath = "assets/" + elementName.toLowerCase() + "_640.mp4";
```

becomes:

```txt
assets/fire_640.mp4
```

<p style="color:#fbbf24;"><strong>💡 Why <code>hide()</code>?</strong> The video exists in the webpage, but we hide it because we only want to draw it inside the canvas text.</p>

</details>

<details>
<summary><strong>🔌 <span style="color:#34d399;">Step 4: Call <code>setupInteractive()</code> In <code>sketch.js</code></span></strong></summary>

Now open `sketch.js`.

Find your `setup()` function.

Before this step, it should look something like this:

```js
// old code ⬇️
function setup() {
  createCanvas(canvasWidth, canvasHeight);

  video = createCapture(VIDEO, { flipped: true });
  video.size(canvasWidth, canvasHeight);
  video.hide();

  handPose.detectStart(video, gotHands);
}
// old code ⬆️
```

Add `setupInteractive();` after `video.hide();` and before `handPose.detectStart(...)`.

```js
// new code ⬇️
function setup() {
  createCanvas(canvasWidth, canvasHeight);

  video = createCapture(VIDEO, { flipped: true });
  video.size(canvasWidth, canvasHeight);
  video.hide();

  // new code ⬇️
  setupInteractive();
  // new code ⬆️

  handPose.detectStart(video, gotHands);
}
// new code ⬆️
```

This works because `index.html` loads `interactive.js` before `sketch.js`.

Check that the bottom of `index.html` has this order:

```html
<script src="interactive.js"></script>
<script src="sketch.js"></script>
```

</details>

<details>
<summary><strong>🔠 <span style="color:#a78bfa;">Step 5: Replace Plain Text With Video-Filled Text</span></strong></summary>

Go back to `interactive.js`.

Find the old Part 03 `drawCurrentElement()` function.

It should look like this:

```js
// old code ⬇️
function drawCurrentElement() {
  let elementTextSize = lerp(minElementTextSize, maxElementTextSize, elementScaleAmount);

  textAlign(CENTER, CENTER);
  textStyle(BOLD);
  textSize(elementTextSize);
  fill(255);
  stroke(0);
  strokeWeight(8);
  text(getCurrentElement(), width / 2, height / 2);
  textAlign(LEFT, BASELINE);
  textStyle(NORMAL);
}
// old code ⬆️
```

Replace the whole old function with this new version:

```js
// new code ⬇️
function drawCurrentElement() {
  let elementTextSize = lerp(minElementTextSize, maxElementTextSize, elementScaleAmount);

  elementTextLayer.clear();

  let currentElement = getCurrentElement();
  let currentVideo = elementVideos[currentElement];

  elementTextLayer.image(currentVideo, 0, 0, width, height);

  elementTextLayer.drawingContext.globalCompositeOperation = "destination-in";
  elementTextLayer.textAlign(CENTER, CENTER);
  elementTextLayer.textStyle(BOLD);
  elementTextLayer.textSize(elementTextSize);
  elementTextLayer.fill(255);
  elementTextLayer.noStroke();
  elementTextLayer.text(getCurrentElement(), width / 2, height / 2);
  elementTextLayer.drawingContext.globalCompositeOperation = "source-over";

  image(elementTextLayer, 0, 0);

  textAlign(LEFT, BASELINE);
  textStyle(NORMAL);
}
// new code ⬆️
```

The key part is:

```js
let currentElement = getCurrentElement();
let currentVideo = elementVideos[currentElement];
```

This means:

```txt
Get the current word.
Use that word to choose the matching video.
```

So if the current word is `"WIND"`, then:

```js
elementVideos[currentElement]
```

means:

```js
elementVideos["WIND"]
```

The masking line is:

```js
elementTextLayer.drawingContext.globalCompositeOperation = "destination-in";
```

That means: keep the video only where the text exists.

Think of `elementTextLayer` as a hidden stack:

1. Draw the whole video on the hidden layer.
2. Switch to `destination-in`.
3. Draw the word on top.
4. Only the video pixels inside the word survive.
5. Draw the hidden layer onto the main canvas.

![Diagram showing the video layer being clipped by a text mask, then drawn onto the main canvas](images/video-text-mask-layers.svg)

<p style="color:#fbbf24;"><strong>⚠️ Common mistake:</strong> Do not create <code>currentVideo</code> twice. Only use <code>let currentVideo = elementVideos[currentElement];</code> one time inside this function.</p>

</details>

<details>
<summary><strong>🔊 <span style="color:#facc15;">Step 6: Add Audio Files To The Same Setup Loop</span></strong></summary>

Now we add sound using the same pattern as the videos.

Inside `setupInteractive()`, find the video setup loop.

Right now it should look like this:

```js
// old code ⬇️
function setupInteractive() {
  for (let i = 0; i < elements.length; i++) {
    let elementName = elements[i];

    // switch out video for each element
    let videoPath = "assets/" + elementName.toLowerCase() + "_640.mp4";

    elementVideos[elementName] = createVideo(videoPath);
    elementVideos[elementName].hide();
    elementVideos[elementName].volume(0);
    elementVideos[elementName].loop();
  }

  elementTextLayer = createGraphics(width, height);
}
// old code ⬆️
```

Add the audio setup inside the same loop, right after the video setup.

Then add the click listener after `elementTextLayer = createGraphics(width, height);`.

Your updated `setupInteractive()` should look like this:

```js
// new code ⬇️
function setupInteractive() {
  for (let i = 0; i < elements.length; i++) {
    let elementName = elements[i];

    // switch out video for each element
    let videoPath = "assets/" + elementName.toLowerCase() + "_640.mp4";

    elementVideos[elementName] = createVideo(videoPath);
    elementVideos[elementName].hide();
    elementVideos[elementName].volume(0);
    elementVideos[elementName].loop();

    // new code ⬇️
    // switch out audio for each element
    let audioPath = "assets/" + elementName.toLowerCase() + ".mp3";

    elementAudios[elementName] = new Audio(audioPath);
    elementAudios[elementName].loop = true;
    elementAudios[elementName].volume = 0;
    // new code ⬆️
  }

  elementTextLayer = createGraphics(width, height);

  // new code ⬇️
  window.addEventListener("pointerdown", function () {
    audioStarted = true;

    for (let i = 0; i < elements.length; i++) {
      elementAudios[elements[i]].play();
    }
  }, { once: true });
  // new code ⬆️
}
// new code ⬆️
```

Browsers usually block sound until the user clicks once.

That is why we use:

```js
window.addEventListener("pointerdown", function () {
```

After that first click, all the audio files start playing silently at volume `0`.

</details>

<details>
<summary><strong>🎚️ <span style="color:#c084fc;">Step 7: Turn Up Only The Current Element Sound</span></strong></summary>

Now go back to `drawCurrentElement()` in `interactive.js`.

Find this part:

```js
let currentElement = getCurrentElement();
let currentVideo = elementVideos[currentElement];

elementTextLayer.image(currentVideo, 0, 0, width, height);

// new audio code goes here

elementTextLayer.drawingContext.globalCompositeOperation = "destination-in";
```

Put the audio volume code between the video image line and the mask line.

```js
// new code ⬇️
if (audioStarted === true) {
  for (let i = 0; i < elements.length; i++) {
    let elementName = elements[i];

    if (elementName === currentElement) {
      elementAudios[elementName].volume = elementScaleAmount;
    } else {
      elementAudios[elementName].volume = 0;
    }
  }
}
// new code ⬆️
```

So that section of `drawCurrentElement()` should now look like this:

```js
let currentElement = getCurrentElement();
let currentVideo = elementVideos[currentElement];

elementTextLayer.image(currentVideo, 0, 0, width, height);

// new code ⬇️
if (audioStarted === true) {
  for (let i = 0; i < elements.length; i++) {
    let elementName = elements[i];

    if (elementName === currentElement) {
      elementAudios[elementName].volume = elementScaleAmount;
    } else {
      elementAudios[elementName].volume = 0;
    }
  }
}
// new code ⬆️

elementTextLayer.drawingContext.globalCompositeOperation = "destination-in";
```

This loop checks every element:

1. If the element is the current word, turn its sound up.
2. If the element is not the current word, turn its sound down to `0`.

The left hand controls volume because we use:

```js
elementScaleAmount
```

for the current sound volume.

</details>

---

<details>
<summary><strong>✅ <span style="color:#6ee7b7;">Complete <code>interactive.js</code></span></strong></summary>

At the end of Part 04, your `interactive.js` should look like this:

```js
let elements = ["EARTH", "WIND", "FIRE", "WATER"];
let currentElementIndex = 0;

// text scaling vars
let elementScaleAmount = 0.5;
let minElementTextSize = 12;
let maxElementTextSize = 360;

// video vars
let elementVideos = {};
let elementTextLayer;

// audio vars
let elementAudios = {};
let audioStarted = false;

function setupInteractive() {
  for (let i = 0; i < elements.length; i++) {
    let elementName = elements[i];

    // switch out video for each element
    let videoPath = "assets/" + elementName.toLowerCase() + "_640.mp4";

    elementVideos[elementName] = createVideo(videoPath);
    elementVideos[elementName].hide();
    elementVideos[elementName].volume(0);
    elementVideos[elementName].loop();

    // switch out audio for each element
    let audioPath = "assets/" + elementName.toLowerCase() + ".mp3";

    elementAudios[elementName] = new Audio(audioPath);
    elementAudios[elementName].loop = true;
    elementAudios[elementName].volume = 0;
  }

  elementTextLayer = createGraphics(width, height);

  window.addEventListener("pointerdown", function () {
    audioStarted = true;

    for (let i = 0; i < elements.length; i++) {
      elementAudios[elements[i]].play();
    }
  }, { once: true });
}

// go to the next element, looping back to the first element if at the end
function advanceElement() {
  currentElementIndex++;

  if (currentElementIndex >= elements.length) {
    currentElementIndex = 0;
  }
}

function getCurrentElement() {
  return elements[currentElementIndex];
}

function updateElementScale(scaleAmount) {
  if (scaleAmount === null) {
    return;
  }

  elementScaleAmount = scaleAmount;
}

// draw the current element, including its video and text mask
function drawCurrentElement() {
  let elementTextSize = lerp(minElementTextSize, maxElementTextSize, elementScaleAmount);

  elementTextLayer.clear();

  let currentElement = getCurrentElement();
  let currentVideo = elementVideos[currentElement];

  elementTextLayer.image(currentVideo, 0, 0, width, height);

  if (audioStarted === true) {
    for (let i = 0; i < elements.length; i++) {
      let elementName = elements[i];

      if (elementName === currentElement) {
        elementAudios[elementName].volume = elementScaleAmount;
      } else {
        elementAudios[elementName].volume = 0;
      }
    }
  }

  elementTextLayer.drawingContext.globalCompositeOperation = "destination-in";
  elementTextLayer.textAlign(CENTER, CENTER);
  elementTextLayer.textStyle(BOLD);
  elementTextLayer.textSize(elementTextSize);
  elementTextLayer.fill(255);
  elementTextLayer.noStroke();
  elementTextLayer.text(getCurrentElement(), width / 2, height / 2);
  elementTextLayer.drawingContext.globalCompositeOperation = "source-over";

  image(elementTextLayer, 0, 0);

  textAlign(LEFT, BASELINE);
  textStyle(NORMAL);
}
```

</details>

<details>
<summary><strong>🧩 <span style="color:#f0abfc;">Why This Works</span></strong></summary>

The element word chooses the video:

```js
let currentElement = getCurrentElement();
let currentVideo = elementVideos[currentElement];
```

The same element word also chooses the audio:

```js
elementAudios[elementName].volume = elementScaleAmount;
```

So the right hand chooses the current element, and the left hand controls how big and loud it is.

</details>

<details open>
<summary><strong>⚠️ <span style="color:#facc15;">Checkpoint: Test It In The Browser</span></strong></summary>

<p style="color:#facc15;"><strong>⚠️ Before moving on:</strong> Open the project with Go Live. Click once on the page so the audio can start.</p>

Then test:

1. Use the right hand switch to move through `EARTH`, `WIND`, `FIRE`, and `WATER`.
2. The word should change.
3. The video inside the word should match the word.
4. The sound should match the word.
5. Move your left thumb and pointer closer together. The word should get smaller and the sound should get quieter.
6. Move your left thumb and pointer farther apart. The word should get bigger and the sound should get louder.

</details>
